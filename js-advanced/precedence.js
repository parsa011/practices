var title = "book";

function title() {
	console.log("title : book");
}

// console will show book value, (the variable value), but if we do not assignt any value to the variable,
// console will show that function
console.log(title);

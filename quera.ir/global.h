#ifndef _GLOBAL_H
# define _GLOBAL_H

#include <iostream>
#include <math.h>

using namespace std;

static void get_array(int arr[], int len)
{
    for (int i = 0; i < len; i++)
        cin >> arr[i];
}

#endif
